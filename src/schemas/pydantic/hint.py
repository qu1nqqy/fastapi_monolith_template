"""
Define Pydantic models for request and response validation in API endpoints.

Use `BaseModel` for serialization/deserialization, validation, and OpenAPI generation.

Separate domain-level models into files and ensure types are clean and strict.
"""
