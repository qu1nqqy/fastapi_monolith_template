"""
Store SQLAlchemy ORM models representing your database schema.

Each model should inherit from Base and reflect a real table with fields, relationships, and constraints.

Follow OOP principles and encapsulate logic per model when necessary.
"""
