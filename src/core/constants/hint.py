"""
Store and organize your application-wide constants here.

For example:
- Enum values
- Configuration keys
- Default values

Keep constants logically grouped by domain and separate into files if needed (e.g., redis.py, db.py).
"""
