"""
Define and group reusable FastAPI dependencies here.

Dependencies should return shared services, repositories, or perform authentication logic.

Used with `Depends(...)` across routers and services.
"""
